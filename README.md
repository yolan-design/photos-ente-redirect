# photos-ente-redirect

Use `&k=` or `&h=` instead of `#` for Instagram because they can't get their URL parser to work correctly with the `#` character.

Simple workaround by redirecting the URL to the correct one.
